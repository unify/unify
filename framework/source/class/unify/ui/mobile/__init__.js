/** 
 * Controls are low-level interface components (visual only) for
 * being used in less capable devices e.g. mobiles or for
 * other requirements with near-zero load times (large portal pages).
 */
